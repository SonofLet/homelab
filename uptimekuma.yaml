services:
  uptime-kuma:
    image: louislam/uptime-kuma:2.1.0
    volumes:
      - /home/kletson/appdata/uptimekuma/data:/app/data
    ports:
      # <Host Port>:<Container Port>
      - 3001:3001
    restart: unless-stopped
